const $=s=>document.querySelector(s);const key='origin-story-session';const clean=s=>(s||'').replace(/해리 포터|마블|디즈니|스타워즈|원피스|나루토|귀멸|지브리|픽사|넷플릭스/gi,'고유한 창작 요소').trim();const seed=s=>[...s].reduce((n,c)=>n+c.charCodeAt(0),0);const pick=(a,n)=>a[Math.abs(n)%a.length];let session;try{session=JSON.parse(localStorage.getItem(key)||'null')}catch{}if(!session?.data){$('#continue-empty').innerHTML='<div class="empty-icon">✦</div><h2>먼저 이야기를 기획해 주세요</h2><p><a href="index.html">기획 페이지로 이동하기 →</a></p>';$('#next-episode').disabled=true}else{const data=session.data;$('#continue-title').textContent=data.title+' · 다음 편';$('#previous-story').value=session.contentText||'';function count(){const n=$('#previous-story').value.replace(/\\s/g,'').length;$('#continue-count').textContent=n.toLocaleString()+' / 20,050';$('#next-episode').disabled=n>20050}function cast(){const own=clean(data.character),hero=own?own.split(/[,.\\n]/)[0].trim():pick(['서린','도윤','하진','유나','지호','민서'],seed(data.title)),other=['시우','가온','다온','태린','아린'].find(x=>x!==hero&&!own.includes(x));return{hero,other}}function episode(number,previous){const {hero,other}=cast(),keys=(clean(data.keywords)||'낯선 신호, 약속').split(',').map(x=>x.trim()),a=keys[0],b=keys[1]||'약속';return '<h2>'+data.title+'</h2><div class="meta">제 '+number+'화</div><p>지난 이야기에서 남겨진 선택은 아직 끝나지 않았다. '+hero+'은(는) '+a+'에 얽힌 흔적을 따라 조용히 발걸음을 옮겼다.\\n\\n그때 '+other+'이(가) '+b+'에 관한 뜻밖의 사실을 전했다. 두 사람은 같은 장면을 보고도 서로 다른 결론에 닿았고, 그 차이는 다음 선택을 더욱 어렵게 만들었다.\\n\\n바람이 멎은 자리에서 '+hero+'은(는) 오래 망설였다. 하지만 이번에는 물러서지 않기로 했다. 아직 답은 보이지 않았지만, 다음 문을 열기에는 충분한 이유가 생겼다.</p>'}function setLabel(){const next=(session.chapter||0)+1;$('#next-label').textContent='제 '+next+'화';$('#next-episode').innerHTML='<span>✦</span> 제 '+next+'화 만들기'}$('#previous-story').oninput=count;$('#next-episode').onclick=()=>{const previous=$('#previous-story').value;if(previous.replace(/\\s/g,'').length>20050)return;const number=(session.chapter||0)+1,html=episode(number,previous),text=$('<div>');text.innerHTML=html;session={data,chapter:number,contentText:text.innerText};localStorage.setItem(key,JSON.stringify(session));$('#continue-content').innerHTML=html;$('#continue-result-label').textContent='EPISODE '+number;$('#continue-empty').hidden=true;$('#continue-result').hidden=false;$('#previous-story').value=session.contentText;count();setLabel();$('#continue-result').scrollIntoView({behavior:'smooth',block:'start'})};$('#continue-copy').onclick=async()=>{await navigator.clipboard.writeText($('#continue-content').innerText);$('#continue-copy').textContent='복사됨';setTimeout(()=>$('#continue-copy').textContent='복사',1200)};count();setLabel()}